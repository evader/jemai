\n\n- **`context`** is the folder where Docker looks for files (including the Dockerfile).\n- **`dockerfile`** is the Dockerfile name (default is `Dockerfile`).\n\n---\n\n### From what you shared earlier:\n\n- Your main Docker build context is `/opt/jemai_prod/docker_setup` or `~/jemai_docker_setup`\n- The Dockerfile is usually named either:\n  - `Dockerfile`\n  - or something like `lt-backend.Dockerfile` (if you have multiple services).\n\n---\n\n### What to do now?\n\n- **Open your main `docker-compose.yml`** (e.g., `/opt/jemai_prod/docker_setup/docker-compose.yml` or `~/jemai_docker_setup/docker-compose.yml`).\n- Find the service you want to fix (likely `jemai_core` or `lt-backend`).\n- Check the `build:` section \u2014 it shows the Dockerfile name and context folder.\n- That folder + Dockerfile is your *main Docker build file*.\n\n---\n\n### Example: if your `docker-compose.yml` has:\n\n