\n# --- JEM AI CLI INSTANT FIXER ---\n# 1. Remove old aliases (from .bashrc and .bash_aliases)\nsed -i '/alias jemai=/d' ~/.bashrc ~/.bash_aliases 2>/dev/null\nsed -i '/alias jem=/d' ~/.bashrc ~/.bash_aliases 2>/dev/null\n\n# 2. Ensure ~/bin exists and is in PATH\nmkdir -p ~/bin\nif ! echo \"$PATH\" | grep -q \"$HOME/bin\"; then\n    echo 'export PATH=\"$HOME/bin:$PATH\"' >> ~/.bashrc\n    export PATH=\"$HOME/bin:$PATH\"\nfi\n\n# 3. Write jem script\ncat <<'EOF' > ~/bin/jem\n#!/bin/bash\nif [ $# -eq 0 ]; then\n  echo \"Usage: jem <your question or command>\"\n  exit 1\nfi\ncurl -s -X POST http://localhost:11436/chat \\\n  -H \"Content-Type: application/json\" \\\n  -d \"{\\\"query\\\":\\\"$*\\\"}\" | jq -r '.response // .error // .message'\nEOF\n\nchmod +x ~/bin/jem\n\n# 4. Unalias in current session (if needed)\nunalias jemai 2>/dev/null\nunalias jem 2>/dev/null\nhash -r\n\n# 5. Test output and final instructions\necho -e \"\\033[1;32m[\u2713] jem command is ready. Open a new terminal or run: source ~/.bashrc\\033[0m\"\necho -e \"\\033[1;36mTry: jem what is the weather in Perth?\\033[0m\"\n