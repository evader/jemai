\n# Start in main control directory on new shell\ncd \"$JEMAI_PROD_DIR\"\n\n# Node Version Manager (optional, safe to leave)\nexport NVM_DIR=\"$HOME/.nvm\"\n[ -s \"$NVM_DIR/nvm.sh\" ] && \\. \"$NVM_DIR/nvm.sh\"\n[ -s \"$NVM_DIR/bash_completion\" ] && \\. \"$NVM_DIR/bash_completion\"\n\n# Optionally load Synapz presence functions if you want (comment if not needed)\nfi       # <-- THIS is the problem! Remove this line completely.\nsource venv/bin/activate\n\n# Add aliases\nif [ -f ~/.bash_aliases ]; then\n    . ~/.bash_aliases\nfi\n