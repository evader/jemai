\n\n---\n\n## 3\ufe0f\u20e3 Open WebUI Configuration\n\n### For **Open WebUI** (web browser, e.g. http://localhost:8080 or http://localhost:3000):\n\n- **Default Open WebUI uses Ollama as its backend.**\n- To allow full Synapz Core access (sudo, shell, etc.), you must:\n  1. Ensure Synapz Core is running:  \n     `sudo systemctl status synapz_core`\n  2. Ensure endpoints like `/chat`, `/shell`, `/writefile` are accessible at `http://localhost:11436`.\n  3. If Open WebUI allows adding **custom endpoints**, add:\n