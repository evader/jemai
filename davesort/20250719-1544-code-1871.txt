\n\n---\n\n### Key notes:\n\n- Flask app runs with `socketio.run(app, host='0.0.0.0', port=5001)` in `lt.py`.\n- Dockerfile exposes 5001 (internal container port).\n- Compose maps host 5000 -> container 5001 for browser access at `http://localhost:5000`.\n- Keep your Ollama, open-webui, etc. services as-is (adjust if you want).\n- Make sure your `requirements.txt` is up to date.\n\n---\n\n### To run:\n\n1. Place `docker-compose.yml`, `lt-backend.Dockerfile`, `lt.py`, `requirements.txt`, and `templates/` in the same folder.\n2. From that folder, run:\n