\ndef refresh_ollama_models():\n    # Checks model folder and tells user what\u2019s available\n    print(\"Rescanning Ollama models:\")\n    if MODELS_ROOT.exists():\n        for f in MODELS_ROOT.glob(\"*\"):\n            print(f\"- {f.name}\")\n    else:\n        print(\"[ERR] Ollama model dir missing.\")\n\n# Add to CLI loop:\nelif typ == \"ai\" and \"ollama\" in arg.lower() and \"refresh\" in arg.lower():\n    refresh_ollama_models()\n