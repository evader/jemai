\nimport json\nfrom langchain_community.vectorstores import Chroma\nfrom langchain_community.embeddings import HuggingFaceEmbeddings\n\n# Load your conversation history JSON\nwith open('your_convo_history.json', 'r') as f:\n    chat_history = json.load(f)\n\n# Flatten if needed, and prep documents\ndocs = []\nfor item in chat_history['messages']:  # adapt this to your JSON structure\n    docs.append(item['content'])       # or whatever key holds the text\n\n# Set up embeddings and ChromaDB\nembedding_func = HuggingFaceEmbeddings(model_name=\"all-MiniLM-L6-v2\")\nvector_db = Chroma.from_texts(docs, embedding_func, persist_directory=\"db_convo_history\")\n\nprint(\"Imported conversation history into ChromaDB!\")\n