\n#!/bin/bash\n\n# Stop Ollama cleanly\nsudo systemctl stop ollama\n\n# Remove current models directory (if it exists)\nsudo rm -rf /usr/share/ollama/.ollama/models\n\n# Re-create expected directory structure\nsudo mkdir -p /usr/share/ollama/.ollama/models/blobs\nsudo mkdir -p /usr/share/ollama/.ollama/models/manifests\n\n# Copy all blobs (might take time, but guarantees *something* is there)\nfind /home/jemai/ai_offline_setup_downloads/ai_models/blobs -type f -exec sudo cp {} /usr/share/ollama/.ollama/models/blobs/ \\;\n\n# Copy all manifests (these are usually tiny, so blast 'em all over)\nif [ -d \"/home/jemai/ai_offline_setup_downloads/ai_models/manifests\" ]; then\n  find /home/jemai/ai_offline_setup_downloads/ai_models/manifests -type f -exec sudo cp {} /usr/share/ollama/.ollama/models/manifests/ \\;\nfi\n\n# Set ownership just in case\nsudo chown -R ollama:ollama /usr/share/ollama/.ollama/models\n\n# Fire up Ollama again\nsudo systemctl start ollama\n\necho \"Shotgun copy done. Try: ollama list\"\n