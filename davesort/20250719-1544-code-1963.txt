\n\nYour HuggingFace embeddings initialization in the RAG tries to load the model on CUDA (GPU) but your system either:\n\n- Does not have an NVIDIA GPU installed, or  \n- The NVIDIA driver is missing or not correctly installed in your host or Docker environment.\n\n---\n\n### How to fix this:\n\n#### Option 1: Use CPU for embeddings (simplest quick fix)\n\nEdit your `jemai_core.py` **replace** this line inside `initialize_rag()`:\n\n