\n#!/bin/bash\n\n# 1. Stop Ollama\nsudo systemctl stop ollama\n\n# 2. Make sure target dirs exist and are clean\nsudo rm -rf /usr/share/ollama/.ollama/models\nsudo mkdir -p /usr/share/ollama/.ollama/models/blobs\nsudo mkdir -p /usr/share/ollama/.ollama/models/manifests\nsudo chown -R ollama:ollama /usr/share/ollama/.ollama/models\n\n# 3. Copy up to 5 biggest blobs from your \"clean\" backup\nSRC_BLOBS=$(find /home/jemai/ai_offline_setup_downloads/ai_models/blobs -type f -size +500M 2>/dev/null | head -n 5)\nfor f in $SRC_BLOBS; do\n    sudo cp \"$f\" /usr/share/ollama/.ollama/models/blobs/\ndone\n\n# 4. Copy any manifests (if found)\nSRC_MANIFESTS=$(find /home/jemai/ai_offline_setup_downloads/ai_models/manifests -type f 2>/dev/null)\nfor f in $SRC_MANIFESTS; do\n    sudo cp \"$f\" /usr/share/ollama/.ollama/models/manifests/\ndone\n\n# 5. Fix permissions\nsudo chown -R ollama:ollama /usr/share/ollama/.ollama/models\n\n# 6. Start Ollama\nsudo systemctl start ollama\n\necho \"Quick model patch complete! Check with: ollama list\"\n