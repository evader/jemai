services:
      lt-backend:
        build:
          context: .
          dockerfile: ./Dockerfile
        ports:
          - "5000:5001" # <--- CHANGED FROM 5000:5000
        environment:
          # ... (your GOOGLE_API_KEY) ...
        volumes:
          - ./lt.py:/app/lt.py
          # ... (rest of configuration) ...