if [ "$GPU_TYPE" == "nvidia" ]; then
        echo "-------------------------------------------------------------------"
        # ... your skip logic and then the CUDA Installer command ...
        echo "-------------------------------------------------------------------"
        read dummy
        echo "-------------------------------------------------------------------"
    fi