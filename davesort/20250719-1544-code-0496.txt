// Inside commandInput.addEventListener('keypress', e => { ... }
        if (e.key === 'Enter') {
            e.preventDefault();
            directButton.click(); // Default to direct run on Enter
        }